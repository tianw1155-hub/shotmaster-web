# ShotMaster 阶段 3.3 组件收敛 + 令牌清理 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 提取 3 个共享组件（HeroBack/PlanSections/ChapterHeader），迁移全部 45 处兼容令牌 → 新名并移除兼容映射，修复 5 个积累 Minor。

**Architecture:** 逐任务：先提取组件（DRY），再全站令牌迁移 sweep（机械替换），最后 Minor 修复。每任务 grep 验证 + check/build 绿。

**Tech Stack:** React 18 + TS + Tailwind 3 + framer-motion + lucide（已有）。分支 `phase3.3-cleanup`，基线 `master`（`cbc1462`）。

## Global Constraints

- **令牌迁移表**：`text/bg/border-primary`→`accent`；`text/bg-mint`→`ink-muted`；`text/bg-sun`→`gold`；`text/bg-sky`→`ink-muted`；`text/bg-grape`→`ink-muted`。章节语境用 `chapter.composition/light/color/narrative/master`。
- **jp-minimal**：washi/sumi/朱/金 + Geist + lucide stroke 1.25 + 无 emoji + 发丝线 + 不堆卡片。
- **范围**：仅清理。不含 admin/Lottie/快门/构图概念。

---

## Task 1: 提取 HeroBack + 替换 3 处

**Files:**
- Create: `src/components/ui/HeroBack.tsx`
- Modify: `src/pages/LevelDetail.tsx`、`src/pages/Gallery.tsx`、`src/pages/Learn.tsx`

- [ ] **Step 1: 创建 HeroBack.tsx**

```tsx
import { ChevronLeft } from 'lucide-react';
interface HeroBackProps { onClick: () => void; className?: string; }
export function HeroBack({ onClick, className = '' }: HeroBackProps) {
  return (
    <button onClick={onClick} aria-label="返回"
      className={`absolute top-4 left-4 w-10 h-10 rounded-full bg-surface-card/80 backdrop-blur flex items-center justify-center shadow-elevated hover:bg-surface-card transition-colors z-10 ${className}`}>
      <ChevronLeft className="w-5 h-5 text-ink" strokeWidth={1.25} />
    </button>
  );
}
```

- [ ] **Step 2: 替换 3 处用法**

在 `LevelDetail.tsx`(~line 285)、`Gallery.tsx`(~line 597)、`Learn.tsx`(~line 261)：找到悬浮返回 `<button>`（`absolute top-4 left-4 w-10 h-10 rounded-full bg-surface-card/80 ...`），替换为 `<HeroBack onClick={() => navigate(-1)} />`。加 `import { HeroBack } from '../components/ui/HeroBack';`。

- [ ] **Step 3: 验证 + 提交**

Run: `npm run check 2>&1 | tail -5 && npm run build 2>&1 | tail -3`
Expected: 绿。
```bash
git add src/components/ui/HeroBack.tsx src/pages/LevelDetail.tsx src/pages/Gallery.tsx src/pages/Learn.tsx
git commit -m "refactor: extract HeroBack, replace 3 duplicated usages"
```

## Task 2: 提取 PlanSections + 替换 2 处

**Files:**
- Create: `src/components/lesson/PlanSections.tsx`
- Modify: `src/pages/LevelDetail.tsx`、`src/pages/Gallery.tsx`

- [ ] **Step 1: 创建 PlanSections.tsx**

提取 LevelDetail/Gallery 中重复的 `planSections` 数组 + 折叠渲染 + 赞/踩。组件接收 `plan`/`isAnalyzing`/`imageId`/`onLike`/`onDislike`/`getFeedback` props，内部管理 `expandedSection` state。包含 6 sections (scene/lighting/composition/params/postProcessing/equipment)，每 section 可展开 + 赞/踩。用 jp-minimal 令牌（accent/gold/ink-muted/章节色；无 emoji；lucide stroke 1.25）。

```tsx
import React, { useState } from 'react';
import { Sun, Lightbulb, Grid3X3, Camera, Palette, Package, ChevronDown, ChevronUp, ThumbsUp, ThumbsDown, Loader2, Sparkles } from 'lucide-react';
import type { ShootingPlan, ShootingPlanDimension } from '../../types';

interface Props {
  plan: ShootingPlan | null;
  isAnalyzing: boolean;
  onLike: (dim: ShootingPlanDimension) => void;
  onDislike: (dim: ShootingPlanDimension) => void;
  getFeedback: (dim: ShootingPlanDimension) => { liked: boolean; disliked: boolean };
}
export function PlanSections({ plan, isAnalyzing, onLike, onDislike, getFeedback }: Props) {
  const [expanded, setExpanded] = useState<string | null>('scene');
  // ... planSections array (6 sections, jp-minimal tokens, lucide stroke 1.25, no emoji)
  // ... accordion render with expand/collapse + thumbs up/down
  // (Extract the JSX from LevelDetail/Gallery's planSections.map, applying the migration table.)
}
```

> **实现指引**：从 `LevelDetail.tsx`(~line 184) 或 `Gallery.tsx`(~line 337) 复制 `planSections` 数组 + `.map()` 渲染。迁移令牌：`text-primary`→`text-accent`；`text-yellow-500`→`text-gold`；`text-sky`→`text-ink-muted`；`text-mint`→`text-ink-muted`；`text-grape`→`text-ink-muted`；`bg-gray-50`→`bg-surface-muted`；`rounded-xl/2xl`→`rounded-md`；emoji `🤖`/`💡`→lucide `Sparkles`/`Lightbulb`。赞/踩 `bg-green-100/text-green-600`→`bg-success/12 text-success`；`bg-red-100/text-red-500`→`bg-danger/12 text-danger`。

- [ ] **Step 2: 替换 LevelDetail + Gallery**

在 `LevelDetail.tsx` + `Gallery.tsx`：移除内联 `planSections` 数组 + `.map()` 渲染，替换为 `<PlanSections plan={shootingPlan} isAnalyzing={isAnalyzing} onLike={handleLikeDimension} onDislike={handleDislikeDimension} getFeedback={(dim) => getDimensionFeedback(image.id, dim)} />`。保留 `handleLike`/`handleDislike`/`getDimensionFeedback` 逻辑（传入 props）。

- [ ] **Step 3: 验证 + 提交**

Run: `npm run check && npm run build` — 绿。
```bash
git add src/components/lesson/PlanSections.tsx src/pages/LevelDetail.tsx src/pages/Gallery.tsx
git commit -m "refactor: extract PlanSections, replace 2 duplicated AI-plan accordions"
```

## Task 3: 提取 ChapterHeader + 替换 LevelMap

**Files:**
- Create: `src/components/ui/ChapterHeader.tsx`
- Modify: `src/pages/LevelMap.tsx`

- [ ] **Step 1: 创建 ChapterHeader.tsx**

```tsx
interface ChapterHeaderProps { name: string; color: string; completed: number; total: number; }
export function ChapterHeader({ name, color, completed, total }: ChapterHeaderProps) {
  const pct = Math.round((completed / total) * 100);
  return (
    <div className="flex items-center gap-3.5 py-3.5 border-b border-line last:border-b-0">
      <span className="w-1.5 h-1.5 rounded-full" style={{ background: color }} />
      <span className="text-[13px] flex-1">{name}<span className="block text-[10px] text-ink-muted font-mono mt-0.5">{String(completed).padStart(2,'0')} / {total}</span></span>
      <span className="w-14 h-[2px] bg-line rounded-full overflow-hidden"><span className="block h-full bg-ink" style={{ width: `${pct}%` }} /></span>
      <span className="text-[10px] text-ink-muted font-mono w-7 text-right">{pct}</span>
    </div>
  );
}
```

- [ ] **Step 2: 替换 LevelMap 章节列表**

在 `LevelMap.tsx`：找到章节列表 `.map(ch => ...)` 内联的 hairline 行，替换为 `<ChapterHeader name={ch.chapter.name} color={ch.chapter.color} completed={done} total={ch.levels.length} />`。

- [ ] **Step 3: 验证 + 提交**

Run: `npm run check && npm run build` — 绿。
```bash
git add src/components/ui/ChapterHeader.tsx src/pages/LevelMap.tsx
git commit -m "refactor: extract ChapterHeader, replace LevelMap inline"
```

## Task 4: 兼容令牌迁移 sweep + 移除 tailwind 映射

**Files:**
- Modify: `ErrorBoundary.tsx`、`ProgressBar.tsx`、`Button.tsx`(mint variant)、`Badge.tsx`(colors)、`GameComponents.tsx`、`InteractiveLesson.tsx`(bg-sky)、`Login.tsx`、`LevelDetail.tsx`、`Gallery.tsx`、`LevelMap.tsx`(残留)
- Modify: `tailwind.config.js`（移除兼容映射）

- [ ] **Step 1: 全站 grep + 替换**

Run: `grep -rnE "text-primary|bg-primary|border-primary|text-mint|bg-mint|text-sun|bg-sun|text-sky|bg-sky|text-grape|bg-grape" src/ --include="*.tsx" | grep -v node_modules`

按迁移表替换每一处：
- `ErrorBoundary.tsx`: `bg-primary`→`bg-accent`, `rounded-2xl`→`rounded-md`, `text-white`→`text-surface`
- `ProgressBar.tsx`: color map `primary`→`accent`(or remove, use `ink`), `mint`→`ink-muted`, `sun`→`gold`, `grape`→`ink-muted`
- `Button.tsx`: `mint` variant → `accent` variant (or remove mint, callers use accent)
- `Badge.tsx`: `primary`→`accent`, `mint`→`ink-muted`, `sun`→`gold`, `sky`→`ink-muted`, `grape`→`ink-muted`
- `GameComponents.tsx`: ~15 处 `text-primary`→`text-accent`, `bg-mint`→`bg-accent`, `text-sun`→`text-gold`, `bg-primary`→`bg-accent`, `bg-sun/10`→`bg-gold/10`, `bg-primary/5`→`bg-accent/5`, `text-gray-200`→`text-line`, `rounded-2xl/3xl/4xl`→`rounded-md`, `bg-gray-100`→`bg-surface-muted`, `border-gray-50`→`border-line`
- `InteractiveLesson.tsx`: `bg-sky`→`bg-ink-muted` (LEVEL_COLOR.under)
- `Login.tsx`: `bg-primary`→`bg-accent`, `text-primary`→`text-accent`, `focus:ring-primary/30`→`focus:ring-accent/30`, `focus:border-primary`→`focus:border-accent`, `border-gray-200`→`border-line`, `bg-gray-50`→`bg-surface-muted`, `placeholder-ink-light`→`placeholder-ink-muted`
- `LevelDetail.tsx`/`Gallery.tsx`: planSections colors (if not fully migrated in Task 2) → complete migration
- `LevelMap.tsx`: any residual compat tokens

- [ ] **Step 2: 移除 tailwind.config 兼容映射**

在 `tailwind.config.js` 的 `colors` 内移除 `primary`/`mint`/`sun`/`sky`/`grape` 对象（5 个 compat 块）。

- [ ] **Step 3: grep 验证 + check/build + 提交**

Run: `grep -rnE "text-primary|bg-primary|text-mint|bg-mint|text-sun|bg-sun|text-sky|bg-sky|text-grape|bg-grape" src/ --include="*.tsx" | grep -v node_modules | head`
Expected: **空**。
Run: `npm run check && npm run build` — 绿。
```bash
git add -A
git commit -m "refactor: migrate all compat tokens + remove tailwind compat mappings"
```

## Task 5: Minor 修复

**Files:**
- Modify: `src/components/ui/Badge.tsx`、`src/components/layout/AppShell.tsx`、`src/components/layout/Sidebar.tsx`、`tailwind.config.js`、`src/stores/useGameStore.ts`、`src/components/lesson/InteractiveLesson.tsx`、`src/pages/Community.tsx`、`src/pages/Profile.tsx`、`src/pages/LevelMap.tsx`
- Create: `src/lib/navSection.ts`

- [ ] **Step 1: Badge 加 ink-muted 色**

`Badge.tsx`：在 `colors` map 加 `'ink-muted': 'bg-ink-muted/10 text-ink-muted'`。将 BadgeColor 类型加 `'ink-muted'`。

- [ ] **Step 2: deriveActive/deriveSection 去重**

Create `src/lib/navSection.ts`:
```ts
export type NavSection = 'levels' | 'gallery' | 'community' | 'learn' | 'profile';
export function deriveSection(pathname: string): NavSection {
  if (pathname.startsWith('/gallery')) return 'gallery';
  if (pathname.startsWith('/community')) return 'community';
  if (pathname.startsWith('/learn')) return 'learn';
  if (pathname.startsWith('/profile')) return 'profile';
  return 'levels';
}
```
`AppShell.tsx` + `Sidebar.tsx`：移除各自的 `deriveActive`/`deriveSection`，`import { deriveSection } from '../../lib/navSection'`。Sidebar 从 BottomNav 移除 `NavSection` 导出（改从 navSection.ts 导入）。

- [ ] **Step 3: hairline hex**

`tailwind.config.js`：`boxShadow.hairline` 的 `#E2E2DE` → `#E6E4DE`。

- [ ] **Step 4: 默认头像**

`src/stores/useGameStore.ts`：找到默认 avatar `📷`（~line 365），改为 `'摄'`。

- [ ] **Step 5: 清未用 import**

grep + 移除：`clamp` (InteractiveLesson.tsx)、`Card` (Community/Profile if unused)、`X`/`ChevronRight`/`TrendingUp` (Community if unused)、`Lock`/`ChevronRight` (LevelMap if unused)。

- [ ] **Step 6: 验证 + 提交**

Run: `npm run check && npm run build` — 绿。
```bash
git add -A
git commit -m "fix: Badge ink-muted + navSection dedup + hairline + avatar + unused imports"
```

## Task 6: 全量验证

- [ ] **Step 1: 全量**

Run: `npm run test && npm run check && npm run build`
Expected: 全过。

- [ ] **Step 2: grep 最终确认**

Run: `grep -rnE "text-primary|bg-primary|text-mint|bg-mint|text-sun|bg-sun|text-sky|bg-sky|text-grape|bg-grape|📷|🎯|📌|⚠️|🏆|🔥|🤖|💡" src/ --include="*.tsx" | grep -v node_modules | head`
Expected: 空（或仅 achievementIconMap dict keys — 数据非渲染，延后）。

- [ ] **Step 3: 手动 UAT + 提交**

`npm run dev`：全站浏览（首屏/学习/图库/社区/个人/详情/拍摄/评分）确认不回归 + jp-minimal 一致。
```bash
git add -A && git commit -m "chore: phase 3.3 complete — component convergence + token cleanup"
```

---

## Self-Review（已执行）

- **Spec 覆盖**：HeroBack(T1)✓ PlanSections(T2)✓ ChapterHeader(T3)✓ 令牌迁移+移除映射(T4)✓ Badge ink-muted(T5)✓ deriveActive去重(T5)✓ hairline(T5)✓ 头像(T5)✓ 未用import(T5)✓ 验证(T6)✓。延后(chapterInfo/achievementIconMap emoji)✓。
- **占位符**：T2 PlanSections 用"实现指引"（从现有复制+迁移）而非完整重写——因其从 2 个大文件提取，完整重写不现实。指引精确（行号+迁移表+具体令牌）。
- **类型一致**：`HeroBackProps`/`PlanSectionsProps`/`ChapterHeaderProps`/`NavSection`/`deriveSection` 一致。
