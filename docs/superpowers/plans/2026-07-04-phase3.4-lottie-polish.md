# ShotMaster 阶段 3.4 Lottie + 打磨 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Lottie 庆祝动画（RewardModal + 3星 confetti）+ 性能（路由 code-split + 图片 lazy + Lottie 按需）+ a11y（focus + skip-to-content + 对比修复）。

**Architecture:** `createConfettiData(count)` 程序化生成 Lottie JSON（无需下载）；`<Confetti>` 组件 lazy 加载 lottie-react + reduced-motion fallback；路由 `React.lazy` + `Suspense` + `PageSkeleton`；a11y focus 管理 + 对比修复。

**Tech Stack:** React 18 + TS + Vite + lottie-react（已装）+ framer-motion（已装）。分支 `phase3.4-lottie-polish`，基线 `master`（`057fa82`）。

## Global Constraints

- **Lottie**：`lottie-react` lazy 加载（`React.lazy(() => import('lottie-react'))`）；`prefers-reduced-motion` 下不渲染 Lottie（CSS/静态 fallback）。
- **性能**：路由 `React.lazy` + `<Suspense fallback={<PageSkeleton />}>`；图片 `loading="lazy"`（hero 除外）；初始包目标 < 600KB。
- **a11y**：`ink-muted` `#8A8A86` → `#6B6B66`（WCAG AA）；路由切换 focus `<main>`；Skip-to-content 链接。
- **jp-minimal**：washi/sumi/朱/金 + Geist + lucide 1.25 + 无 emoji。
- **范围**：Lottie + perf + a11y。不含 admin ④。

---

## Task 1: Lottie 基建（createConfettiData + Confetti 组件，TDD）

**Files:**
- Create: `src/lib/lottie.ts`, `src/lib/lottie.test.ts`, `src/components/ui/Confetti.tsx`

**Interfaces:**
- Produces: `createConfettiData(count: number, options?) => LottieData`（返回 Lottie JSON 对象）；`<Confetti count={N} className? />`（lazy lottie-react + reduced-motion fallback）。

- [ ] **Step 1: 写 lib/lottie.ts — createConfettiData**

```ts
interface LottieShapeLayer {
  ty: 4; nm: string; sr: 1;
  ks: { o: { a: number; k: any[] }; p: { a: number; k: any[] }; s: { a: number; k: number[] } };
  shapes: any[]; ip: number; op: number; st: number; bm: number;
}
interface LottieData { v: string; fr: number; ip: number; op: number; w: number; h: number; layers: LottieShapeLayer[]; }

const COLORS = [
  [0.7, 0.29, 0.23, 1],   // 朱
  [0.6, 0.47, 0.24, 1],   // 金
  [0.1, 0.1, 0.1, 1],     // 墨
  [0.42, 0.56, 0.5, 1],   // 鼠尾草
  [0.37, 0.48, 0.63, 1],  // 石板
];

export function createConfettiData(count = 30, w = 400, h = 400): LottieData {
  const fr = 60; const duration = 90; // 1.5s
  const layers: LottieShapeLayer[] = [];
  for (let i = 0; i < count; i++) {
    const x = Math.round(Math.random() * w);
    const size = 8 + Math.round(Math.random() * 12);
    const color = COLORS[i % COLORS.length];
    const delay = Math.round(Math.random() * 20);
    const drift = Math.round((Math.random() - 0.5) * 120);
    const endX = x + drift;
    layers.push({
      ty: 4, nm: `p${i}`, sr: 1,
      ks: {
        o: { a: 1, k: [{ t: delay, s: [100], i: { x: [0.5], y: [1] }, o: { x: [0.5], y: [0] } }, { t: delay + 50, s: [0] }] },
        p: { a: 1, k: [{ t: delay, s: [x, -20, 0], i: { x: 0.5, y: 1 }, o: { x: 0.5, y: 0 }, to: [drift / 3, h / 3, 0], ti: [-drift / 3, -h / 3, 0] }, { t: delay + 60, s: [endX, h + 20, 0] }] },
        s: { a: 0, k: [100, 100, 100] },
      },
      shapes: [
        { ty: 'el', p: { a: 0, k: [0, 0] }, s: { a: 0, k: [size, size] } },
        { ty: 'fl', c: { a: 0, k: color } },
      ],
      ip: delay, op: delay + 60, st: 0, bm: 0,
    });
  }
  return { v: '5.7.4', fr, ip: 0, op: duration, w, h, layers };
}
```

- [ ] **Step 2: 写 lottie.test.ts**

```ts
import { describe, it, expect } from 'vitest';
import { createConfettiData } from './lottie';
describe('createConfettiData', () => {
  it('returns valid Lottie structure', () => {
    const d = createConfettiData(10);
    expect(d.v).toBe('5.7.4');
    expect(d.layers).toHaveLength(10);
    expect(d.layers[0].ty).toBe(4); // shape layer
    expect(d.layers[0].shapes.length).toBeGreaterThanOrEqual(2); // ellipse + fill
  });
  it('default count 30', () => {
    expect(createConfettiData().layers).toHaveLength(30);
  });
});
```

- [ ] **Step 3: 跑测试**

Run: `npm run test -- lottie.test 2>&1 | tail -5`
Expected: PASS (2).

- [ ] **Step 4: 写 Confetti.tsx**

```tsx
import React from 'react';
import { createConfettiData } from '../../lib/lottie';

const Lottie = React.lazy(() => import('lottie-react'));

export function Confetti({ count = 30, className = '' }: { count?: number; className?: string }) {
  const [data] = React.useState(() => createConfettiData(count));
  const [reduced] = React.useState(() => window.matchMedia('(prefers-reduced-motion: reduce)').matches);
  if (reduced) return null;
  return (
    <React.Suspense fallback={null}>
      <Lottie animationData={data} loop={false} className={className} />
    </React.Suspense>
  );
}
```

- [ ] **Step 5: 验证 + 提交**

Run: `npm run check 2>&1 | tail -5 && npm run build 2>&1 | tail -3`
Expected: 绿。
```bash
git add src/lib/lottie.ts src/lib/lottie.test.ts src/components/ui/Confetti.tsx
git commit -m "feat(lottie): createConfettiData + lazy Confetti component"
```

## Task 2: RewardModal Lottie 庆祝

**Files:**
- Modify: `src/components/game/GameComponents.tsx`（RewardModal）

- [ ] **Step 1: 替换 RewardModal 的 CSS 星动画为 Lottie Confetti**

在 `RewardModal` 内：在星级显示区域上方叠加 `<Confetti count={40} className="absolute inset-0 pointer-events-none" />`。保留 XP/星数/连胜统计 + 按钮（仅替换星动画的视觉效果——Lottie confetti 叠加在上方）。移除 `animate-star-pop`/`animate-bounce-in` CSS（保留静态星级 SVG + bounce-in 容器可选）。import `Confetti`。

- [ ] **Step 2: 验证 + 提交**

Run: `npm run check && npm run build` — 绿。
```bash
git add src/components/game/GameComponents.tsx
git commit -m "feat(reward): Lottie confetti celebration in RewardModal"
```

## Task 3: Score-reveal 3 星完美 confetti 叠加

**Files:**
- Modify: `src/components/score/ScoreResultView.tsx`

- [ ] **Step 1: 3 星时叠加 Confetti**

在 `ScoreResultView` 的星级区域，当 `score.stars === 3` 且 `revealed` 时，叠加 `<Confetti count={30} className="absolute inset-0 pointer-events-none z-10" />`。其他星级不叠加。import `Confetti`。

- [ ] **Step 2: 验证 + 提交**

Run: `npm run check && npm run build` — 绿。
```bash
git add src/components/score/ScoreResultView.tsx
git commit -m "feat(score): 3-star confetti overlay on perfect reveal"
```

## Task 4: 性能（路由 code-split + 图片 lazy）

**Files:**
- Modify: `src/App.tsx`（React.lazy 页面组件）、Create: `src/components/ui/PageSkeleton.tsx`
- Modify: 全站 `<img>` 加 `loading="lazy"`（hero 除外）

- [ ] **Step 1: 创建 PageSkeleton**

```tsx
// src/components/ui/PageSkeleton.tsx
export function PageSkeleton() {
  return (
    <div className="min-h-screen bg-surface flex items-center justify-center">
      <div className="w-8 h-8 border-2 border-line border-t-accent rounded-full animate-spin" />
    </div>
  );
}
```

- [ ] **Step 2: App.tsx React.lazy + Suspense**

在 `App.tsx`：用 `React.lazy` 包装所有页面组件（LevelMap/Learn/Gallery/Community/Profile/LevelDetail/Shoot/Score/Login/Preferences/admin）。在 `<AnimatePresence>` 内的 `<Routes>` 外包 `<Suspense fallback={<PageSkeleton />}>`。

```tsx
const LevelMapPage = React.lazy(() => import('./pages/LevelMap').then(m => ({ default: m.LevelMapPage })));
// ... 对每个页面组件同样 lazy
```

- [ ] **Step 3: 图片 lazy**

grep 全站 `<img` 无 `loading=` 的，加 `loading="lazy"`（LevelMap hero 除外——它是首屏可见，用 `loading="eager"`）。

- [ ] **Step 4: 验证初始包 + 提交**

Run: `npm run build 2>&1 | grep -E "dist/assets|kB" | head` — 确认初始 chunk < 600KB。
Run: `npm run check && npm run build` — 绿。
```bash
git add -A
git commit -m "perf: route code-split (React.lazy) + image lazy-load + PageSkeleton"
```

## Task 5: a11y（focus + skip-to-content + 对比 + ARIA）

**Files:**
- Modify: `tailwind.config.js`（ink-muted hex）、`src/components/layout/AppShell.tsx`（skip-to-content + focus）、`src/components/layout/PageLayout.tsx`（main tabIndex + id）
- Modify: 全站补 `aria-label`（grep 图标按钮无 aria-label 的）

- [ ] **Step 1: ink-muted 对比修复**

`tailwind.config.js`：`ink.muted` `#8A8A86` → `#6B6B66`。

- [ ] **Step 2: PageLayout main focusable**

`PageLayout.tsx`：`<main>` 加 `id="main-content" tabIndex={-1}`。

- [ ] **Step 3: AppShell skip-to-content + focus on route change**

`AppShell.tsx`：在 `<MotionConfig>` 内最前面加 `<a href="#main-content" className="sr-only focus:not-sr-only focus:absolute focus:top-2 focus:left-2 focus:z-50 focus:bg-accent focus:text-white focus:px-3 focus:py-2 focus:rounded-md">跳到主要内容</a>`。加 `useEffect` 在 `location.pathname` 变化时 `document.getElementById('main-content')?.focus()`。

- [ ] **Step 4: ARIA 审计**

grep 图标按钮无 `aria-label` 的：`grep -rn "<button" src/ --include="*.tsx" | grep -v "aria-label" | grep -v "children" | head`。给纯图标按钮补 `aria-label`（HeroBack 已有；补 Slider/LevelMap nodes 等）。

- [ ] **Step 5: 验证 + 提交**

Run: `npm run check && npm run build` — 绿。
```bash
git add -A
git commit -m "fix(a11y): ink-muted contrast + focus management + skip-to-content + ARIA"
```

## Task 6: 全量验证

- [ ] **Step 1: 全量**

Run: `npm run test && npm run check && npm run build`
Expected: 全过。

- [ ] **Step 2: 构建包大小确认**

Run: `npm run build 2>&1 | grep "kB" | head`
Expected: 初始 chunk < 600KB。

- [ ] **Step 3: 手动 UAT + 提交**

`npm run dev`：通关看 RewardModal Lottie confetti；3星评分看 confetti 叠加；路由切换看 code-split 加载 + focus；Tab 键看 skip-to-content。
```bash
git add -A && git commit -m "chore: phase 3.4 complete — lottie + perf + a11y"
```

---

## Self-Review（已执行）

- **Spec 覆盖**：Lottie RewardModal(T2)✓ score-reveal 3星(T3)✓ code-split+lazy(T4)✓ focus+skip+contrast+ARIA(T5)✓ Lottie infra(T1)✓ 验证(T6)✓。
- **占位符**：无 TBD；createConfettiData 完整代码；Confetti 组件完整。
- **一致性**：`createConfettiData`(T1) → `<Confetti>`(T1) → RewardModal(T2)/ScoreResultView(T3) 一致；PageSkeleton(T4) → App Suspense(T4) 一致。
